<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    /* General reset */
    body, html {
      margin: 0;
      padding: 0;
      width: 100%;
      overflow-x: hidden; /* Prevent horizontal scroll */
    }
    .container {
      width: 100%;
      max-width: 600px;
      margin: auto;
      padding: 15px;
      background-color: #ffffff;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-sizing: border-box; /* Ensures padding is included in width calculation */
    }
    .header {
      background-color: #007bff;
      color: white;
      padding: 15px;
      text-align: center;
      border-top-left-radius: 5px;
      border-top-right-radius: 5px;
      box-sizing: border-box;
    }
    .footer {
      margin-top: 15px;
      padding: 15px;
      background-color: #f8f9fa;
      border-bottom-left-radius: 5px;
      border-bottom-right-radius: 5px;
      text-align: center;
      box-sizing: border-box;
    }
    .content {
      box-sizing: border-box;
    }
    /* Responsive adjustments */
    @media (max-width: 600px) {
      .container {
        padding: 10px;
      }
      .header, .footer {
        padding: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    {{> header }} <!-- This includes the header partial -->
    <div class="content">
      {{{body}}} <!-- This is where the content of the child template will be injected -->
    </div>
    {{> footer }} <!-- This includes the footer partial -->
  </div>
</body>
</html>
